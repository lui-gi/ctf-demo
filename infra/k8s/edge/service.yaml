---
apiVersion: v1
kind: Service
metadata:
  name: edge
  namespace: progctf-fleet
  labels:
    app.kubernetes.io/name: edge
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: edge
  ports:
    - name: http
      port: 80
      targetPort: http
