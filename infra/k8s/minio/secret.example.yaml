---
# EXAMPLE ONLY — manage real values via Sealed Secrets / Vault.
#   kubectl -n progctf-fleet create secret generic minio-credentials \
#     --from-literal=rootUser=progctf \
#     --from-literal=rootPassword="$(openssl rand -base64 48)"
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: progctf-fleet
type: Opaque
stringData:
  rootUser: progctf
  rootPassword: REPLACE_AT_DEPLOY_TIME
