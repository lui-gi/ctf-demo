slug: first_mates_burner
category: crows_nest
difficulty: port
isolation: shared
resources:
  cpu: "0.25"
  memory: "128Mi"
ports: [8080]
ttl_idle_minutes: 0   # shared instance — no idle reaping
env:
  # No CHALLENGE_FLAG injection: this is a static-site Island where the flag
  # is hard-baked into one of the served HTML pages. The flag IS the bio of
  # the alt-account profile at /u/seafoam_smuggler/index.html. Cartographer
  # spec'd it that way (see hint_for_powder_monkey).
  PROGCTF_FLAG_LOCATION: "/u/seafoam_smuggler/ bio"
