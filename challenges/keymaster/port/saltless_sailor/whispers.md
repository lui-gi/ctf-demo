# Whispers — The Saltless Sailor

## Whisper 1 (-10%)
The hash is one of the oldest, fastest, and most-broken algorithms in the catalogue. Identify it by length.

## Whisper 2 (-20% cumulative)
40 hex characters means SHA-1 (`hashcat -m 100`). Crack against the included `pirates.txt` wordlist — the plaintext IS the flag.
